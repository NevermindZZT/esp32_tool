#
# This is a project Makefile. It is assumed the directory this Makefile resides in is a
# project subdirectory.
# Note: This legacy Makefile is kept for reference. The project now uses CMake-based build.
# Use `idf.py build` for building.
#

PROJECT_NAME := esp32-tool

include $(IDF_PATH)/make/project.mk

